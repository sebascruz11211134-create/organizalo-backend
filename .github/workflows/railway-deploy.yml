# Railway deploy is handled via direct GitHub integration (not GitHub Actions).
# This workflow is disabled.
name: Deploy to Railway (disabled)

on:
  workflow_dispatch: # manual only

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Railway deploys automatically via GitHub integration."
